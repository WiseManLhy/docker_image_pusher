nginx
#支持私库
k8s.gcr.io/kube-state-metrics/kube-state-metrics:v2.0.0
xhofe/alist:latest
#支持指定架构
--platform=linux/arm64 xiaoyaliu/alist

# Jenkins 官方镜像（自动拉取 latest 标签）
jenkins/jenkins

# Jenkins LTS 版本 + Linux amd64 架构
--platform=linux/amd64 jenkins/jenkins:lts-jdk17

# Jenkins 基础镜像
jenkins/jenkins:lts

# 自动化部署工具（如 Helm、kubectl）
alpine/helm:latest
bitnami/kubectl:latest

# 指定架构的镜像（如 ARM64 平台）
--platform=linux/arm64 jenkins/inbound-agent:latest
